# 任务准备状态修正报告

## 【Task #26 修正前是否可调度】

**是。** 上一轮补全 `files_to_modify` 后，`find_runnable_tasks(56)` 返回 `[Task#26]`。
因为当时 Scheduler 只检查：status=pending + 依赖空 + 无lease + files_to_modify非空 + task_type≠manual。
Task #26 全部满足 → 安全矛盾成立。

---

## 【新增 readiness_status】

| 项目 | 说明 |
|------|------|
| 字段名 | `readiness_status` |
| 默认值 | `draft` |
| 有效值 | `draft`, `needs_planning`, `ready`, `executing`, `testing`, `completed`, `blocked` |
| DB位置 | `development_tasks` 表 |
| ORM | `app/models/task.py` 新增 Column |
| API输出 | `_task_out()` 返回 `readiness_status` 字段 |

---

## 【项目56的12个任务状态】

| # | 任务 | readiness_status |
|---|------|:---:|
| 26 | 搭建Electron项目基础框架 | `needs_planning` |
| 27 | 商品采集模块 - 拼多多平台抓取 | `needs_planning` |
| 28 | 商品采集模块 - 抖店平台抓取 | `needs_planning` |
| 29 | 商品采集模块 - 小红书平台抓取 | `needs_planning` |
| 30 | 竞品分析模块 - 多维度对比与图表生成 | `needs_planning` |
| 31 | 图片编辑与管理模块 - 基本编辑功能 | `needs_planning` |
| 32 | 图片编辑与管理模块 - 批量处理 | `needs_planning` |
| 33 | 一键发布模块 - 拼多多发布 | `needs_planning` |
| 34 | 一键发布模块 - 抖店发布 | `needs_planning` |
| 35 | 一键发布模块 - 小红书发布 | `needs_planning` |
| 36 | 本地数据管理模块 - 数据搜索与过滤 | `needs_planning` |
| 37 | 本地数据管理模块 - 数据导出 | `needs_planning` |

全部 `needs_planning`。原因：
- 尚未绑定经过验证的项目代码仓库
- 文件路径尚未经过真实目录扫描确认
- 涉及Electron、采集平台和外部服务
- 不适合作为第一次模型试运行

---

## 【项目6 Task #38状态】

| 字段 | 值 |
|------|-----|
| id | 38 |
| title | 新增商品标题规范化函数并补充测试 |
| status | `pending` |
| readiness_status | **`ready`** |
| dependencies | `[]` （无依赖） |
| files_to_modify | `["module_demo.py", "test_module_demo.py"]` |
| task_type | `code` |
| active leases | 0 |
| resource locks | 0 |

---

## 【Scheduler 真实返回】

```
project_id=56:
  find_runnable_tasks → 0 个
  全部12个 blocked，原因：尚未完成工程规划

project_id=6:
  find_runnable_tasks → 1 个
  唯一 runnable: Task #38「新增商品标题规范化函数并补充测试」
```

调度器新增准入条件：
```python
# SQL WHERE clause
WHERE status = 'pending'
  AND readiness_status = 'ready'    # ← 新增安全门
```

---

## 【前端显示】

### 项目56顶部：
> **12个任务尚未完成工程规划，不能自动执行**

### 每张任务卡：
> 标签 `等待工程规划`（黄色 badge-warning）

### 项目6 Task #38：
> 标签 `已准备，可执行`（绿色 badge-success）

### 点"确认启动"时的Toast：
> `12个任务尚未完成工程规划，不能自动执行`

---

## 【活跃 run / lease / resource lock】

| 类型 | 数量 |
|------|:---:|
| active task_leases | 0 |
| active executor_resource_locks | 0 |
| executor_runs（活跃中） | 0（7条历史记录均有 finished_at，已终止） |
| integrity_check | ok |
| foreign_key_check | 0 |

---

## 【是否可以点击项目56开始：NO】

12个任务全部 `needs_planning`，Scheduler 返回 `runnable=0`。

## 【是否可以点击项目6沙箱开始：YES】

仅 Task #38 符合所有准入条件：
- status=pending
- readiness_status=ready
- 无依赖
- 无lease
- files_to_modify 指向真实文件
- task_type=code
- 工作区存在 (`<EXECUTOR_SANDBOX_ROOT>`)
- 不涉及电商采集、数据库迁移、依赖安装
- 最多修改2个文件
- 有明确pytest测试
- 可随时回滚

---

## 【修改文件清单】

| 文件 | 修改 |
|------|------|
| `backend/app/executor/task_scheduler.py` | `find_runnable_tasks`: SQL 增加 `AND readiness_status='ready'`；`get_queue_status`: 增加 readiness_status 诊断 |
| `backend/app/models/task.py` | 新增 `readiness_status` Column |
| `backend/app/api/tasks.py` | `_task_out()` 返回 `readiness_status` |
| `backend/app/api/executor.py` | `NO_RUNNABLE_TASKS` 诊断增加 `not_ready_count` |
| `frontend/src/api/tasks.ts` | `Task` 接口新增 `readiness_status` |
| `frontend/src/api/executor.ts` | `StartResponse`/`BlockedTaskInfo` 新增 `not_ready_count`/`readiness_status` |
| `frontend/src/pages/ProjectDetail.tsx` | 新增 `READINESS_STATUS_LABEL`/`COLOR`；任务卡 badge；顶部 alert；改进 Toast 消息 |
| DB | `ALTER TABLE development_tasks ADD COLUMN readiness_status`；项目56→`needs_planning`；Task#38→`ready` |
